# Sample 13 - Basic components

A static Stage app that covers the common UI component surface without
backend state: Tailwind layout utilities, Quasar form controls, buttons,
menus, dialogs, notifications, tabs, tables, trees, and progress.

There is no custom CSS and no app-local JavaScript bundle. The sample is
one `main.py` plus one root-level `home.vue`.
